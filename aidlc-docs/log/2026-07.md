# 2026-07 作業ログ

## 2026-07-27: タイマー/ストップウォッチ/天気を専用ビュー化

- 何を: HOME からの「タイマー」「ストップウォッチ」操作を ActionMenu から
  専用フルスクリーンビュー（SCREEN_SLOT）に、タイマー設定をプリセット
  ActionMenu から分秒ピッカー（SCREEN_TIMER_SET）に、「天気」の応答表示を
  ANSWER 共有から専用ビュー（SCREEN_WEATHER）に変更（`src/c/wrist_agent.c`
  のみ）。SPECS.md 3.3b/3.4/3.5 を更新し、ADR-023 を追記。
- なぜ: 「会話履歴だけが専用ビューを持っている状態を、他のメニュー項目にも
  広げてほしい」というユーザー要望。タイマー設定はプリセットでなく分秒
  刻みで指定したいという追加要望も反映。
- ハマりどころ: 特になし。既存の単一 Window + Screen enum 切り替え方式を
  踏襲し、ActionMenu のアクション本体ロジックはそのまま関数として再利用
  できたため設計上の手戻りはなかった。ビルド確認は `pebble` CLI が本
  環境に存在しないため未実施（実機/エミュレータでの確認が別途必要）。

## 2026-07-27: タイマー満了時のバイブをユーザーが止めるまで繰り返す

- 何を: `handle_timer_fired()` の一度きりの `vibes_double_pulse()` を廃止し、
  専用の `SCREEN_ALARM` 画面へ強制遷移した上で `AppTimer` により2秒間隔で
  バイブを繰り返す。SELECT/BACK/UP/DOWN いずれかのボタンで停止（`src/c/
  wrist_agent.c` のみ）。SPECS.md 6.2/3.6 を更新し、ADR-024 を追記。
- なぜ: 一度のバイブでは見逃しやすいというユーザー指摘。
- ハマりどころ: 特になし。既存の Screen 切り替え方式にそのまま乗せられた。

## 2026-07-27: SLOT/タイマー設定/ALARM のレイアウト調整とバイブ間隔短縮

- 何を: SLOT・分秒ピッカー・ALARM の大きな数字/メッセージ表示をコンテンツ
  領域内で上下中央に配置し直し、分秒ピッカーの選択中フィールドのハイライト
  幅を画面半分から数字2桁分の固定幅に縮小、`ALARM_VIBE_INTERVAL_MS` を
  2秒から1秒に短縮（`src/c/wrist_agent.c` のみ）。SPECS.md 3.4/3.5/3.6/6.2
  を更新し、ADR-025 を追記。
- なぜ: レビューで「数字が上下中央になっていない」「バイブ間隔が緩慢」
  「選択カーソルが画面端まで間延びしている」という3点の指摘を受けた。
- ハマりどころ: レイアウト定数を関数内で `#define` すると翻訳単位全体に
  漏れるため、`const int` のローカル変数に置き換えた。

## 2026-07-27: タイマー設定ピッカーの数字が "..." になる回帰を修正

- 何を: 直前のコミットでハイライト幅を50pxに縮めたところ、
  `FONT_KEY_BITHAM_42_BOLD` の2桁数字が収まらず省略記号 `...` になる
  回帰が発生。`tset_digit_w` を50→70pxに拡大して修正（`src/c/
  wrist_agent.c` のみ）。ADR-026 を追記。
- なぜ: ユーザーからの実機/エミュレータでの表示崩れ報告。
- ハマりどころ: レイアウト調整（ADR-025）で見た目を優先し、フォントが
  実際に必要とする描画幅の検証が不足していた。次回以降、幅を縮める
  変更をする際はフォントサイズに対する最低限の幅を確認すること。

## 2026-07-29: タイマー確定の確認ビュー化・秒10秒刻み・リセット自動再開廃止・Lap履歴追加

- 何を: (1) タイマー設定の秒を1秒→10秒刻みに、(2) 分秒ピッカーの確定を
  SELECT長押しから専用の SCREEN_TIMER_CONFIRM（確認ビュー、SELECT短押しで
  決定・BACKでやり直し）に変更、(3) SLOTのリセットでタイマーが自動的に
  再スタートしないよう修正（停止状態・元の長さに戻すだけに変更）、
  (4) ストップウォッチのLapを履歴として複数件（最大8件）保持し、
  新設の SCREEN_LAPS（SLOTのDOWN短押しから開く）で一覧表示（`src/c/
  wrist_agent.c` のみ）。SPECS.md 3.4/3.5/3.5b/3.5c/6.1 を更新し、
  ADR-027 を追記。
- なぜ: レビューで「秒10秒刻みでよい」「長押し確定は直感に反する」
  「リセット直後の自動再スタートが不自然」「Lapが複数刻めるのに確認する
  場所がない」の4点を指摘された。
- ハマりどころ: 確認ビューの表示に、幅の狭い分秒ピッカー用レイヤー
  （70px、ADR-026参照）をそのまま使い回そうとして同じ省略記号の問題を
  作りかけたため、SLOT画面のフル幅レイヤーを再利用する形に修正した。

## 2026-07-29: タイマーのリセットで時間を設定し直せるように修正

- 何を: タイマーの SELECT 長押し（従来「リセット」）を、現在の長さを
  プリセットした分秒ピッカーを開く処理に変更。確認画面で値を変えずに
  決定すれば従来の「リセット」相当、値を変えれば新しい時間の再設定に
  なる。`slot_reset()` はSW専用に縮小（`src/c/wrist_agent.c` のみ）。
  SPECS.md 3.4/3.5b を更新し、ADR-028 を追記。
- なぜ: ADR-027 直後のレビューで「リセットしても時刻設定できない、前回の
  時刻に戻るだけ」という指摘を受けた。
- ハマりどころ: `SCREEN_TIMER_CONFIRM` の決定処理が「新規作成」と「既存
  タイマーの編集」を区別する必要があり、`s_open_slot` の状態で判定する
  実装にした。誤判定を避けるため `menu_item_timer()` の新規作成分岐で
  `s_open_slot = -1` を明示するようにした。

## 2026-07-29: 分秒ピッカーで30秒未満を選べてしまう問題を修正

- 何を: `tset_up_click`/`tset_down_click` に `tset_clamp_min_duration()` を
  追加し、分が0のときは秒を30秒未満に選べないようにした（`src/c/
  wrist_agent.c` のみ）。SPECS.md 3.5 を更新し、ADR-029 を追記。
- なぜ: 「10秒を設定しても30秒が設定される」という指摘。Wakeup API の
  30秒未満予約不可という制約により `handle_timer_set()` が黙って
  30秒に切り上げていたが、ピッカー側では選べてしまっていた。
- ハマりどころ: 特になし。ピッカー側の選択肢を制限するだけのシンプルな
  修正で解決した。

## 2026-07-29: 分秒ピッカーで秒編集中に BACK で分に戻れるように

- 何を: `tset_back_click` を編集中フィールドで分岐させ、秒編集中は
  BACK で分編集に戻るだけ（HOMEへは戻らない）に変更。あわせて、
  ADR-027以降ずっと更新されていなかった `s_tset_hint_layer` の固定文言
  （削除済みの長押し確定を指していた）を、編集中フィールドに応じて
  動的に切り替えるよう修正（`src/c/wrist_agent.c` のみ）。SPECS.md 3.5
  を更新し、ADR-030 を追記。
- なぜ: 「秒の設定時に分に戻りたい」という指摘。
- ハマりどころ: 修正中に、ヒント文言が既に古い操作方法（SEL長押しで決定）
  を指したまま放置されていたことに気づいた。次に操作方法を変える際は
  静的なヒント文言も一緒に見直すこと。

## 2026-07-29: タイマーの時間設定で現在の長さをプリフィルしないよう変更

- 何を: タイマーの SELECT長押し（時間設定）で、既存の長さをピッカーに
  引き継ぐのをやめ、新規作成時と同じデフォルト値（5分0秒、
  `TSET_DEFAULT_MINUTES`/`TSET_DEFAULT_SECONDS` として共有）から常に
  開始するよう変更（`src/c/wrist_agent.c` のみ）。SPECS.md 3.4 を更新し、
  ADR-031 を追記。
- なぜ: 「リセットして未確定のままHOMEに戻り再度入ると元の状態を覚えて
  いる。リセットは文字通り初期化されている方が分かりやすい」という指摘。
  ADR-028 の「現在の長さをプリセットする」設計方針を一部撤回する形。
- ハマりどころ: 特になし。新規作成・既存編集の両分岐で同じデフォルト
  定数を参照するよう共有化しただけのシンプルな変更。

## 2026-07-29: 「リセット」と「時間の再設定」をボタンごと分離

- 何を: SELECT長押しを即時確定のリセット（ADR-027の内容に復元）に戻し、
  時間の再設定は未使用だった DOWN ボタン（タイマー側）に移した
  （`src/c/wrist_agent.c` のみ）。SPECS.md 3.4/3.5b を更新し、ADR-032
  を追記。
- なぜ: 「リセットが中途半端。BACKでHOMEに戻ってから再度入ると一時停止中
  のタイマーが復帰している」という指摘。ADR-028以降、SELECT長押しが
  ピッカーを開くだけで即座には何も確定しない設計になっており、リセット
  したつもりでキャンセルすると元の状態が残ってしまっていた。
- ハマりどころ: 「リセット」（即時確定であるべき）と「時間の再設定」
  （多段階の確認フローが妥当）は本質的に異なる操作なので、同じボタンに
  無理に統合しようとしたこと自体がここまでの手戻りの原因だった。次に
  似た設計をするときは、即時確定操作と多段階フローを最初から別ボタンに
  分けることを検討する。

## 2026-07-29: 「リセット」をタイマー・SWとも「完全に削除して未設定に戻す」に統一

- 何を: SELECT長押しの「リセット」を、タイマー・SWとも「スロットを完全に
  削除して未設定に戻す」に統一。タイマーは削除後そのまま分秒ピッカーへ、
  SWは削除後HOMEへ。重複していたBACK長押し（削除）は廃止し、`slot_reset()`
  （SW専用の0秒リセット関数）も削除して`slot_delete()`に一本化。
  `SCREEN_TIMER_CONFIRM`の「既存タイマー編集」分岐も到達不能になったため
  削除（`src/c/wrist_agent.c` のみ）。SPECS.md 3.4/3.5b を更新し、ADR-033
  を追記。
- なぜ: ADR-032後も「リセットしてHOMEに戻り再度入ると一時停止中のタイマー
  が復帰する。未設定に戻ってほしい」という指摘。議論の末、「完全に削除」
  という定義に統一すれば既存の「削除」（BACK長押し）と結果が同じになる
  ため統合することで合意した。
- ハマりどころ: ここまでの一連の手戻り（ADR-027〜033）を振り返ると、
  「リセット」の意味（元の値に戻す／未設定にする／時間を変える）を
  最初にユーザーと厳密に合意せずに実装を進めたことが根本原因だった。
  用語の意味が曖昧な操作を実装する前に、期待される最終状態を具体例で
  確認する方が結果的に速い。

## 2026-07-29: SWのリセットをその場で0秒に戻す方式に、SW新規作成のオートスタートを廃止

- 何を: ADR-033で「タイマー・SWともリセット＝完全削除」に統一した直後、
  SWについてのみ「HOMEに遷移せず、その場で00:00・停止状態に戻る」方式
  （ADR-027/032相当）に戻した。あわせて `menu_item_stopwatch()` の新規
  作成をオートスタートしない（`running=0`で作成）よう変更（`src/c/
  wrist_agent.c` のみ）。SPECS.md 3.4 を更新し、ADR-034 を追記。
- なぜ: 「SWのリセットはホーム遷移を期待しない、その場で00:00・停止で
  よい」「初期状態でストップウォッチに遷移したときにオートスタート
  しなくてよい」という指摘。
- ハマりどころ: 同時に報告された「タイマーの一時停止/再開で設定時間が
  再度始まってしまう」という3点目は、`slot_toggle()` のコードを読む
  限り `remaining` を正しく保存・再スケジュールしており、原因を特定
  できなかった。ユーザーに再現手順を確認してから対応する。

## 2026-07-29: タイマー一時停止→再開で残り30秒未満だと30秒に巻き戻る不具合を修正

- 何を: `schedule_timer_wakeup()` を Wakeup 予約専用の
  `schedule_timer_wakeup_only()` と、target_ts も書き換える従来の
  `schedule_timer_wakeup()`（新規作成/sanitize用）に分割。`slot_toggle()`
  の再開処理は前者を使い、target_ts には本当の残り時間を設定するよう
  修正。あわせて `tick_handler()` に、フォアグラウンド中は Wakeup 通知を
  待たず target_ts 到達を直接検知して満了させる処理を追加（`src/c/
  wrist_agent.c` のみ）。SPECS.md 3.4/6.2 を更新し、ADR-035 を追記。
- なぜ: 「30秒のタイマーを29秒以下で一時停止・再開すると30秒から
  開始する」という指摘。原因は Wakeup API の30秒未満予約不可という
  制約が、画面表示の残り時間にまで（不必要に）伝播していたこと。
- ハマりどころ: `target_ts` が「表示用の残り時間」と「OSへのWakeup予約
  時刻」の二役を兼ねていたため、両者が食い違うケース（30秒未満での
  再開）を分離するのに関数分割が必要だった。`sanitize_slots()` が
  誤って早期にスロットを削除しないか懸念したが、`wakeup_query()` は
  OS側の実際の予約状況を返すため問題ないことを確認した。
